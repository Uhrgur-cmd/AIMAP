{{- if .Values.siemensBrowser.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: siemens-browser
  namespace: {{ .Values.namespace }}
  labels:
    app: siemens-browser
  annotations:
    {{- with .Values.siemensBrowser.ingress.annotations }}
{{ toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.siemensBrowser.ingress.middlewares.enabled }}
    traefik.ingress.kubernetes.io/router.middlewares: >
      {{ .Values.namespace }}-{{ .Values.siemensBrowser.ingress.middlewares.strip.name }}@kubernetescrd
      {{- if .Values.siemensBrowser.ingress.middlewares.apiKey.enabled }}
      ,{{ .Values.namespace }}-{{ .Values.siemensBrowser.ingress.middlewares.apiKey.name }}@kubernetescrd
      {{- end }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.siemensBrowser.ingress.className | default "traefik" }}
  rules:
    - host: {{ .Values.siemensBrowser.ingress.host }}
      http:
        paths:
          - path: {{ .Values.siemensBrowser.ingress.path | default "/opcua" }}
            pathType: {{ .Values.siemensBrowser.ingress.pathType | default "Prefix" }}
            backend:
              service:
                name: siemens-browser
                port:
                  number: {{ .Values.siemensBrowser.port }}
  {{- if .Values.siemensBrowser.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.siemensBrowser.ingress.host }}
      {{- if .Values.siemensBrowser.ingress.tls.secretName }}
      secretName: {{ .Values.siemensBrowser.ingress.tls.secretName }}
      {{- end }}
  {{- end }}
{{- end }}