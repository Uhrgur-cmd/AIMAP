{{- if .Values.siemensBrowser.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: siemens-browser
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector: { matchLabels: { app: siemens-browser } }
  template:
    metadata:
      labels: { app: siemens-browser }
    spec:
      containers:
        - name: app
          image: {{ .Values.siemensBrowser.image | quote }}
          imagePullPolicy: IfNotPresent
          ports: [ { containerPort: {{ .Values.siemensBrowser.port }}, name: http } ]
          readinessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 10
            periodSeconds: 20
          resources: {{- toYaml .Values.siemensBrowser.resources | nindent 12 }}
      nodeSelector: {{- toYaml .Values.siemensBrowser.nodeSelector | nindent 8 }}
      tolerations: {{- toYaml .Values.siemensBrowser.tolerations | nindent 8 }}
      affinity: {{- toYaml .Values.siemensBrowser.affinity | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: siemens-browser
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: siemens-browser }
  ports:
    - name: http
      port: {{ .Values.siemensBrowser.port }}
      targetPort: http
{{- end }}